import { ComponentStyleConfig } from '@chakra-ui/react'

export const CalendarHeader: ComponentStyleConfig = {
  baseStyle: {
    h: 8,
    w: 'full',
    gridRowStart: 1,
    gridColumnStart: 1,
    justifyContent: 'space-between',
  },
}
